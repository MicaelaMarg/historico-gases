import api from "@/api/axios";
import gasesData from "@/components/data";
/*
const parseDate = (str: string) => {
    let [day, month, year] = str.split('/');
    return new Date(`${year}-${month}-${day}`); // formato yyyy-MM-dd
};
*/